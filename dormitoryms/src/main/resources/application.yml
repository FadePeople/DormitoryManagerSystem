server:
  port: 8181
spring:
  datasource:
    driver-class-name:
    url: jdbc:mysql://localhost:3306/dormitory
    username: root
    password: root
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: com/hyb/mapper/xml/*.xml


